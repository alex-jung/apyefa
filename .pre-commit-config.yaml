repos:
  - repo: local
    hooks:
      - id: pipeguard
        name: pipeguard
        entry: pipeguard scan
        language: system
        files: ^\.github/workflows/.*\.(yml|yaml)$
        pass_filenames: false
